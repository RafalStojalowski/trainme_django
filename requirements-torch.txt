# Install this file FIRST before requirements.txt:
#   pip install -r requirements-torch.txt
#
# For CUDA (GPU), replace the --index-url with your CUDA version, e.g.:
#   pip install torch==2.12.0 torchaudio==2.11.0 --index-url https://download.pytorch.org/whl/cu121
torch==2.12.0
torchaudio==2.11.0
--index-url https://download.pytorch.org/whl/cpu
